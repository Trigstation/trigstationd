# Build output
/trigstationd
/trigstationd.exe
/dist/

# Go test and coverage artefacts
*.test
*.test.exe
*.out
coverage.txt

# Local runtime state. A directory instance's database holds only ciphertext,
# but it should never be committed regardless.
*.db
*.db-wal
*.db-shm
/data/

# The operator's deployment settings: their domain, and the source_url of their
# fork. .env.example is the committed template.
/.env

# Editor and OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
